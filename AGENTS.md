# AGENTS.md

## 固定パス（保存先）

- Windows保存先: `C:\YellowMable`
- WSL保存先: `/mnt/c/YellowMable`
- レポート既定出力先: `/mnt/c/YellowMable/00_Raw`
- 環境変数: `YELLOWMABLE_DIR`（未指定時は `/mnt/c/YellowMable`）

## アクセスしやすくするための導線

- リポジトリ直下のシンボリックリンク: `./YellowMable`
  - リンク先: `/mnt/c/YellowMable`
